<div class="row">
  <div class="col-sm-10 offset-sm-1 col-md-8 offset-md-2 col-lg-6 offset-lg-3">
    <div class="card shadow-sm">
      <div class="card-body">
        <h2 class="text-center mb-4">投稿内容</h2>

        <%# エラーメッセージ表示 %>
        <%= render "shared/errors", object: post %>

        <%= form_with model: post, local: true do |f| %>
          <div class="form-group mb-3">
            <%= f.label :occurred_on, "出来事の日付", class: "col-form-label" %>
            <%= f.date_field :occurred_on, class: "form-control", required: true %>
          </div>
          
          <div class="form-group mb-3">
            <%= f.label :body, "エピソード本文", class: "col-form-label" %>
            <%= f.text_area :body, rows: 6, class: "form-control", required: true %>

            <p class="text-muted mt-2" style="font-size: 0.85em;">
              ※ 投稿後、エピソード本文をもとに AI が性格タグを自動でつけてくれます。<br>
              文章がとても短い場合や、行動や気持ちが読み取りづらい内容のときは、
              自動タグ付けが行われないことがあります。
            </p>
          </div>

          <div class="form-group mb-4">
            <%= f.label :profile_ids, "このエピソードの対象", class: "col-form-label" %>
            <fieldset class="border rounded p-3">
              <div class="row">
                  <%= f.collection_check_boxes(:profile_ids, current_user.family.profiles, :id, :name) do |b| %>
                    <div class="col-lg-4 col-6">
                      <%= b.label class: "form-check-label mb-0" do %>
                        <%= b.check_box %>
                        <%= b.text %>
                      <% end %>
                    </div>
                  <% end %>
                </div>
            </fieldset>
          </div>

           <div class="form-group mb-4">
            <%= f.label :profile_ids, "性格タグ", class: "col-form-label" %>
              <small class="text-muted d-block mt-2" style="font-size: 0.85em;">
                AIも利用できますし、以下のリストからタグを選びこともできます。
              </small>
            <fieldset class="border rounded p-3 mb-3">
              <div class="row">
                <%= f.collection_check_boxes(:tag_ids, Tag.active, :id, :name) do |b| %>
                  <div class="col-lg-4 col-6">
                    <%= b.label { b.check_box + b.text } %>
                  </div>
                <% end %>
              </div>
            </fieldset>
          </div>

            <%#= f.label :tag_names, "性格タグ（カンマ区切り）" %>
            <%#= f.text_field :tag_names,
                  value: @post.tag_names,
                  class: "form-control",
                  placeholder: "例）慎重、優しさ、忍耐力" %>
            <!--<small class="text-muted">
              カンマで区切って入力（最大10件）
            </small>-->

          <div class="d-flex justify-content-between mt-4">
            <%= link_to "戻る", posts_path, class: "btn btn-outline-secondary" %>
            <%= f.submit "投稿する", class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
